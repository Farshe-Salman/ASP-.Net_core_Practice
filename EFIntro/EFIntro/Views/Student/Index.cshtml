
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@if (TempData["msg"] != null)
{
    <div class="alert alert-success">@TempData["msg"]</div>

}


@if (Model.Count > 0)
{
    <table class="table table-bordered">
        <tr>
            <th>Name</th>
            <th>Age</th>
            <th>Department</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Age</td>
                <td>@item.Department</td>
                <td><a href="/Student/Details">Details</a>  </td>
                <td><a href="/Student/Update">Update</a>  </td>
                <td><a href="/Student/Delete">Delete</a>  </td>
            </tr>
        }
    </table>
}


